var request = require('request');
var expect = require('chai').expect;

// Should be able to connect to the server.
describe('', function() {
  describe('Basic Server Tests', function() {
    it('Should foo', function(done) {
      expect(1).to.equal(1);
      done();
    });
  });
});

// Should be able to get the index of the server.

// Should be able to fetch all of the products from the server.

// Should be able to search for a product on the server.

// Server should give 404 for pages that don't exist.

